define contactgroup {
       contactgroup_name    <%= @contactgroup_name %>
       shinken_alias        <%= @shinken_alias %>
<%
t_members = Array.new
@members.each do |member|
  t_members.push(member)
end
-%>
       members             <%= t_members.join(",") %>
}
